const input = document.getElementById("input");
input[0].onChange = calculate;
input[1].onChange = calculate;
function calculate(e)
{
    const num1 = document.getElementById("input");
    let sum=0;
    for(let i=0;i<=num1+50;i++)
{
    if(num[i]%2 == 0)
}
num1.foreach(function(e))
{
    sum+=e;
}
const p = document.querySelector(p);
p = innerHTML = 'sum of even numbers' from 5 to 55,sum;